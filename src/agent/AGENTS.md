# src/agent/

This directory contains the core logic for the agentic loop and AI provider integration.

- `loop.ts`: Implements the ReAct-style agentic loop.
- `models.ts`: Configuration and initialization of LLM models.
- `provider.ts`: Logic for handling different AI providers via the AI SDK.
