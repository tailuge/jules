# src/config/

This directory handles application configuration.

- `schema.ts`: Zod schema for configuration validation.
- `loader.ts`: Logic for loading configuration from environment variables or files.
