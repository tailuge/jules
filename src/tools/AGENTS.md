# src/tools/

This directory contains the tools that the agent can use to interact with the system.

- `registry.ts`: Tool registration and discovery.
- `shell.ts`: Tool for executing shell commands.
- `file.ts`: Tool for file system operations.
